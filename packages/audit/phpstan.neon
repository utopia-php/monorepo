includes:
    - ../../phpstan.neon

parameters:
    treatPhpDocTypesAsCertain: false

    # The repair entrypoint ships with the package, so it is analysed too.
    paths:
        - %currentWorkingDirectory%/bin
