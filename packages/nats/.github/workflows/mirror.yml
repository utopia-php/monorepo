name: Mirror

on:
  pull_request_target:
    types: [opened]

permissions:
  pull-requests: write

jobs:
  redirect:
    uses: utopia-php/monorepo/.github/workflows/mirror-redirect.yml@main
    with:
      package: nats
